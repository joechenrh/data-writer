CREATE TABLE mixed_types_table (
    user_id BIGINT,
    username VARCHAR(50),
    score INT,
    rating FLOAT,
    balance DOUBLE,
    registration_date DATE,
    last_login TIMESTAMP,
    is_premium TINYINT
);